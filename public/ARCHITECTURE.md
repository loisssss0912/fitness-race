一旦我所属的文件夹有所变化请更新我。

公共资源目录存放 Vite 直接静态托管的图片和说明。
这里的文件可通过站点根路径访问，例如 `/hero-bg.png`。
新增、替换或删除公共资源时必须同步更新本文档。

- `hero-bg.png`：用户提供的首页“谁是垃圾”底图，优先作为 hero 背景。
- `ARCHITECTURE.md`：本目录架构说明；列出静态资源职责。
- `fight-bg.svg`：原创科幻摔角海报底图，作为 hero 背景回退资源。
- `README-HERO-BG.txt`：首页底图替换说明。
